<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper    
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace속성:매퍼파일의 완전한경로 .xml는 생략 -->
<!-- ※ibatis와는 다르게 id값에 .(dot)를 사용 못한다. -->

<mapper namespace="mybatis.Member">
	<select id="Islogin" parameterType="java.util.Map" resultType="int">
		SELECT COUNT(*) FROM H_MEMBER WHERE ID=#{id} AND PASSWORD=#{password}
	</select>
	<insert id="MemberJoin" parameterType="java.util.Map">
		INSERT INTO H_MEMBER
  		VALUES(#{id},#{password},#{name},#{email},#{address},#{cellphone},
  		#{authority},#{my_comment},#{picture},#{inter_sports},#{joinDate},#{nick})
	</insert>
</mapper>
